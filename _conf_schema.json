{
  "authorization_list": {
    "description": "authorization列表",
    "type": "list",
    "default": [],
    "hint": "支持添加多个Token轮询使用，不要一次性加太多账号，谨防大规模封号。Token获取方式：登录https://chatgpt.com以后，进入https://chatgpt.com/api/auth/session复制accessToken字段的值填写进去即可，头部不要加Bearer "
  },
  "model": {
    "description": "模型代码",
    "type": "string",
    "default": "sy_8",
    "hint": "目前已知可用的只有 sy_8"
  },
  "default_prompt": {
    "description": "默认提示词",
    "type": "string",
    "default": "让图片画面动起来",
    "hint": ""
  },
  "screen_mode": {
    "description": "默认图片方向",
    "type": "string",
    "options": [
      "自动",
      "竖屏",
      "横屏"
    ],
    "default": "自动",
    "hint": "生成图片横屏还是竖屏，如果没有图片且为自动，默认竖屏"
  },
  "sora_base_url": {
    "description": "sora_base_url",
    "type": "string",
    "default": "https://sora.chatgpt.com",
    "hint": "用于发起任务请求，可使用反代绕过地域限制。不用于下载视频"
  },
  "chatgpt_base_url": {
    "description": "chatgpt_base_url",
    "type": "string",
    "default": "https://chatgpt.com",
    "hint": "用于获取Sentinel tokens以绕过反爬限制"
  },
  "speed_down_url_type": {
    "description": "下载视频的反代类型",
    "type": "string",
    "options": [
      "拼接",
      "替换"
    ],
    "default": "拼接",
    "hint": "拼接类似于 https://加速域名/+https://原始域名。替换类似于 https://videos.openai.com/... -> https://加速域名/...，直接替换掉主域名"
  },
  "speed_down_url": {
    "description": "下载视频的加速反代",
    "type": "string",
    "default": "",
    "hint": "如上所述"
  },
  "proxy": {
    "description": "proxy",
    "type": "string",
    "default": "",
    "hint": "HTTP代理，格式可能是http://127.0.0.1:7890，填写后将应用于全部端点请求"
  }
}