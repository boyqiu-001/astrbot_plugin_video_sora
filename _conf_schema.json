{
  "authorization_list": {
    "description": "authorization列表",
    "type": "list",
    "default": [],
    "hint": "支持添加多个Token轮询使用。Token获取方式：登录https://chatgpt.com以后，进入https://chatgpt.com/api/auth/session复制accessToken字段的值填写进去即可，不要加Bearer 前缀"
  },
  "task_limit": {
    "description": "每个账号的并发限制",
    "type": "int",
    "default": "3",
    "hint": "以前2，现在是3，以后可能会变"
  },
  "model": {
    "description": "模型代码",
    "type": "string",
    "default": "sy_8",
    "hint": "目前已知可用的只有 sy_8"
  },
  "default_prompt": {
    "description": "默认提示词",
    "type": "string",
    "default": "生成一个多片段视频",
    "hint": ""
  },
  "screen_mode": {
    "description": "默认视频方向",
    "type": "string",
    "options": [
      "自动",
      "竖屏",
      "横屏"
    ],
    "default": "自动",
    "hint": "生成视频横屏还是竖屏，如果没有图片且为自动，默认竖屏"
  },
  "sora_base_url": {
    "description": "sora_base_url",
    "type": "string",
    "default": "https://sora.chatgpt.com",
    "hint": "用于发起任务请求，可使用反代绕过地域限制。不用于下载视频"
  },
  "chatgpt_base_url": {
    "description": "chatgpt_base_url",
    "type": "string",
    "default": "https://chatgpt.com",
    "hint": "用于获取Sentinel tokens以绕过反爬限制"
  },
  "speed_down_url_type": {
    "description": "下载视频的反代类型",
    "type": "string",
    "options": [
      "拼接",
      "替换"
    ],
    "default": "拼接",
    "hint": "拼接类似于 https://加速域名/+https://原始域名。替换类似于 https://videos.openai.com/... -> https://加速域名/...，直接替换掉原链域名"
  },
  "speed_down_url": {
    "description": "下载视频的加速反代",
    "type": "string",
    "default": "",
    "hint": "如上所述"
  },
  "white_list_enabled": {
    "description": "启用sid白名单",
    "hint": "启用后只有白名单内的sid才能使用该插件",
    "type": "bool",
    "default": false
  },
    "white_list": {
    "description": "sid白名单",
    "type": "list",
    "default": [],
    "hint": "可以通过sid命令获取sid,填写后只有白名单内的sid才能使用该插件"
  },
  "proxy": {
    "description": "proxy",
    "type": "string",
    "default": "",
    "hint": "HTTP代理，格式可能是http://127.0.0.1:7890，填写后将应用于全部端点请求"
  }
}